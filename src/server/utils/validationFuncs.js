export const userHasValidatedData = (values, name) => {
  console.log("User validated data", name);
  return values;
};

export const machineHasValidatedData = (values, name) => {
  console.log("Machine validated data", name);
  return values;
};
