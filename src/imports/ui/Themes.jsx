import { createMuiTheme } from '@material-ui/core/styles';

export const darkTheme = createMuiTheme({
  palette: {
    type: 'dark',
    navigation: {
      main: '#1f1f1f',
      hover: '#404040'
    }
  },
});