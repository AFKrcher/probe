{
  "name": "cosparID",
  "description": "International identifier assigned to artificial objects in space. Consists of the launch year, a 3-digit incrementing launch number of that year and up to a 3-letter code representing the sequential identifier of a piece in a launch.",
  "fields": [
    {
      "name": "reference",
      "hidden": true,
      "type": "url",
      "allowedValues": [],
      "required": true
    },
    {
      "name": "verified",
      "hidden": true,
      "type": "verified",
      "required": true
    },
    {
      "name": "cosparID",
      "type": "string",
      "allowedValues": [],
      "required": true,
      "isUnique": true,
      "stringMax": 255
    }
  ]
}
