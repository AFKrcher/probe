{
  "name": "status",
  "description": "Operational status of the satellite.",
  "fields": [
    {
      "name": "reference",
      "description": "Provide a source or rationale for the provided data",
      "type": "url",
      "allowedValues": [],
      "required": true
    },
    {
      "name": "status",
      "type": "string",
      "allowedValues": [
        "operational",
        "operational with limitations (or standby)",
        "degraded",
        "not operational",
        "functional turned off",
        "no status reported"
      ],
      "required": true
    }
  ]
}
